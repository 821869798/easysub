name: Rust CI

on:
  push:
    branches:
      - "**"
    paths:
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - "src/**"
      - "crates/**"
      - "tests/**"
      - "benches/**"
      - "workdir/**"
      - Dockerfile
      - .dockerignore
      - ".github/workflows/ci.yml"
      - ".github/workflows/release.yml"
  pull_request:
    paths:
      - Cargo.toml
      - Cargo.lock
      - rust-toolchain.toml
      - "src/**"
      - "crates/**"
      - "tests/**"
      - "benches/**"
      - "workdir/**"
      - Dockerfile
      - .dockerignore
      - ".github/workflows/ci.yml"
      - ".github/workflows/release.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  correctness:
    name: Correctness and compatibility
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Install project Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0
        with:
          components: clippy,rustfmt

      - name: Verify active Rust toolchain
        run: |
          rustc --version
          cargo --version
          test "$(rustc --version | awk '{print $2}')" = "1.96.0"

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2

      - name: Check Rust formatting
        run: cargo fmt --all -- --check

      - name: Run Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Run Rust tests, golden corpus, and properties
        run: cargo test --workspace --all-targets

      - name: Verify core has no Axum dependency
        shell: bash
        run: |
          set -euo pipefail
          if cargo tree -p easysub-core -e normal | grep -Eq '(^|[[:space:]])axum v'; then
            echo "easysub-core must not depend on Axum" >&2
            exit 1
          fi

  performance:
    name: Release performance and size
    runs-on: ubuntu-latest
    env:
      EASYSUB_BENCH_MAX_PARSE_1000_NODES_MS: "50"
      EASYSUB_BENCH_MAX_CLASH_EXPORT_1000_NODES_MS: "100"
      EASYSUB_BENCH_MAX_SINGBOX_EXPORT_1000_NODES_MS: "50"
      EASYSUB_BENCH_MAX_GROUP_SELECT_1000_NODES_MS: "20"
      EASYSUB_BENCH_MAX_MRS_10000_DOMAINS_MS: "100"
      EASYSUB_BENCH_MAX_PARSE_10000_MIXED_RULES_MS: "100"
    steps:
      - uses: actions/checkout@v7

      - name: Install project Rust toolchain
        uses: dtolnay/rust-toolchain@1.96.0

      - name: Verify active Rust toolchain
        run: |
          rustc --version
          cargo --version
          test "$(rustc --version | awk '{print $2}')" = "1.96.0"

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2

      - name: Run core performance gates
        run: cargo bench --bench core

      - name: Build release binary
        run: cargo build --release

      - name: Enforce 12 MiB Linux binary limit
        shell: bash
        run: |
          bytes=$(stat -c%s target/release/easysub-rs)
          limit=$((12 * 1024 * 1024))
          echo "release bytes: $bytes"
          test "$bytes" -le "$limit"

  container:
    name: Container build and health
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build local CI image
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: easysub-rs:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Verify container health endpoint
        shell: bash
        run: |
          set -euo pipefail
          docker run --detach --name easysub-rs-ci --publish 25500:25500 easysub-rs:ci
          trap 'docker logs easysub-rs-ci; docker rm --force easysub-rs-ci' EXIT
          for attempt in $(seq 1 30); do
            if curl --fail --silent --output /dev/null http://127.0.0.1:25500/healthz; then
              exit 0
            fi
            sleep 1
          done
          exit 1
